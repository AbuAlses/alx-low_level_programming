/**
 * _islower- Check description
 * Description: It check lowercase fallowed by a new line
 * Return: 0 or 1
 */
int _islower(int c)
{
if (c >= 'a' && c <= 'z')
{
return (1);
}
else
{
return (0);
}
}
